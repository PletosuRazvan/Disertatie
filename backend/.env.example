MONGO_URI=mongodb://localhost:27017/football_predictions
JWT_SECRET_KEY=change-me-before-deploy
SECRET_KEY=another-secret-key
FLASK_DEBUG=true
# Comma-separated allowed API origins (deployed URL in prod, or *).
CORS_ORIGINS=http://localhost:5173
# Where the built React app lives (auto-set inside Docker).
# FRONTEND_DIST=../frontend/dist
